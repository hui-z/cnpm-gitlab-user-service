const assert = require('assert');
const cnpmGitlabUserService = require('../index.js');

describe('cnpmGitlabUserService', () => {
  it('has a test', () => {
    assert(false, 'cnpmGitlabUserService should have a test');
  });
});
